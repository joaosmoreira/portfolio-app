---
import Layout from "../layouts/Layout.astro";
import { siteConfig } from "../data/config";
import { education } from "../data/education";
import EducationItem from "../components/EducationItem.astro";

const recentEducation = education.slice(0, 3);
const olderEducation = education.slice(3);
---

<Layout title="Sobre | João Moreira" theme="light">
  <div class="max-w-5xl mx-auto w-full">
    <header class="mb-12">
      <h1 class="text-4xl font-bold tracking-tighter text-zinc-900 mb-4">
        Sobre Mim
      </h1>
      <p class="text-base md:text-xl text-zinc-600 font-light">
        Do design gráfico e web para o rigor da preparação de obra de estruturas
        metálicas.
      </p>
    </header>

    <div
      class="prose prose-lg max-w-none prose-p:text-zinc-700 prose-p:leading-relaxed"
    >
      <div class="flex flex-col md:flex-row gap-8 items-start mb-16">
        <div
          class="w-full md:w-1/3 aspect-[3/4] relative rounded-2xl overflow-hidden flex-shrink-0 bg-zinc-100 border border-zinc-200"
        >
          <img
            src="/pessoal/moreira.jpg"
            alt="João Moreira"
            class="w-full h-full object-cover"
          />
        </div>

        <div class="w-full md:w-2/3">
          <p class="text-base md:text-lg text-zinc-700 leading-relaxed">
            {siteConfig.bio}
          </p>
        </div>
      </div>

      <div class="space-y-6 mb-16">
        <h2 class="text-2xl font-bold text-zinc-900 tracking-tight m-0">
          Interesses & Cultura
        </h2>
        <div class="flex flex-col gap-4 text-zinc-600">
          <p class="m-0">
            Fora do ambiente rigoroso dos projetos e das obras, mantenho um lado
            altamente dinâmico da minha vida.
          </p>

          <ul class="space-y-4 mt-4 list-none pl-0">
            <li class="flex items-start gap-3">
              <div
                class="w-2 h-2 rounded-full bg-zinc-400 mt-2.5 flex-shrink-0"
              >
              </div>
              <span class="m-0"
                ><strong>Fotografia:</strong> Uma paixão que me permite captar detalhes
                industriais, elementos arquitetónicos e a essência do quotidiano
                através das lentes.</span
              >
            </li>
            <li class="flex items-start gap-3">
              <div
                class="w-2 h-2 rounded-full bg-zinc-400 mt-2.5 flex-shrink-0"
              >
              </div>
              <span class="m-0"
                ><strong>Exploração Tecnológica:</strong> Estou constantemente a
                experimentar e a analisar novas frameworks, gadgets e ferramentas
                digitais que otimizam o dia a dia e os processos de trabalho.</span
              >
            </li>
          </ul>
        </div>
      </div>

      <div class="space-y-8">
        <h2 class="text-2xl font-bold text-zinc-900 tracking-tight m-0">
          Educação
        </h2>
        <div class="columns-1 md:columns-2 md:gap-x-12">
          {
            [...recentEducation, ...olderEducation].map((item) => (
              <EducationItem
                institution={item.institution}
                course={item.course}
                duration={item.duration}
                skills={item.skills}
              />
            ))
          }
        </div>
      </div>
    </div>
  </div>
</Layout>
